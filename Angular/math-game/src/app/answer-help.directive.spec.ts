import { AnswerHelpDirective } from './answer-help.directive';

describe('AnswerHelpDirective', () => {
  it('should create an instance', () => {
    const directive = new AnswerHelpDirective();
    expect(directive).toBeTruthy();
  });
});
