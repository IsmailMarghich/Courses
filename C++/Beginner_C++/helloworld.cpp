#include <iostream> //including standard in/output library

using namespace std; //including the namespace standard library, so we do not have to use std::

int main() {
    cout << "Hello, world" << endl; //we print with c output function
    //endl adds a newline at the end, and also flushes the stream, which means it immediately gets put out
    return 0;
}
